package com.liveroads.ui

import android.support.v7.app.AppCompatActivity

/**
 * An activity that will never be used at runtime but is provided for use as the "tools:content" in layout resource
 * files so that the proper theme will be used in the design tools.
 */
class ToolsActivity : AppCompatActivity()
