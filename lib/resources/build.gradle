apply plugin: "com.android.library"
apply from: rootProject.file("common.gradle")

android {
    publishNonDefault true
}

dependencies {
    compile "com.android.support:design:${versions.appCompat}"
}
